send_to_hero = {
	category = interaction_category_friendly
	common_interaction = yes

	send_name = SEND_PROPOSAL


	desc = send_to_hero_interaction_desc

	is_shown = {
		always = no
		# scope:recipient = {
			# is_in_the_same_court_as = scope:actor
		# }
		# scope:recipient = {
			# is_child_of = scope:actor
		# }
	}

	is_valid_showing_failures_only = {
		scope:recipient = {
			age < 18
			NOT = { has_trait = incapable }
			trigger_if = {
				limit = {
					NOT = { this = scope:actor }
				}
				is_courtier_of = scope:actor
			}
			is_child_of = scope:actor
		}
	}

	can_be_picked = {
		is_at_home = yes
	}
	
	on_accept = {
		
		scope:actor = {
			hidden_effect = {
				send_interface_message = {
					title = send_to_hero_interaction_notification
					left_icon = scope:recipient
				}
			}
		}
	}

	auto_accept = yes
	
	ai_accept = {
		base = 100
	}
	
	ai_targets = {
		ai_recipients = children
	}
	ai_target_quick_trigger = {
		adult = no
	}
	ai_frequency = 250
	
	ai_potential = {
		always = yes
	}
	
	ai_will_do = {
		base = 80
	}
}